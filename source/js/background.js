/* eslint-disable */
/* chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
	  console.log(`Received message ='${JSON.stringify(message)}' sender=${sender} sendResponse=${sendResponse}`)
 });
*/
